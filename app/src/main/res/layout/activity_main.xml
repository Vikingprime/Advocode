<android.support.v4.widget.DrawerLayout
xmlns:android="http://schemas.android.com/apk/res/android"
android:id="@+id/drawer_layout"
android:layout_width="match_parent"
android:layout_height="match_parent">
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin2"
    android:paddingRight="@dimen/activity_horizontal_margin2"
    android:paddingTop="@dimen/activity_vertical_margin2"
    tools:context="com.vuadvocate.advocode2015.StartActivity"
    android:background="@drawable/vumc">

    <ImageView
        android:id="@+id/VUMCtext"
        android:src="@drawable/vumclabel"
        android:layout_width="@dimen/VUMC"
        android:layout_height="@dimen/VUMC"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="@string/slider"
        android:textSize="@dimen/abc_text_size_medium_material"
        android:textColorHighlight="@color/orange"
        android:id="@+id/slide"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
<ListView android:id="@+id/left_drawer"
    android:layout_width="240dp"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:choiceMode="singleChoice"
    android:divider="@android:color/transparent"
    android:dividerHeight="0dp"
    android:background="#111"/>
</android.support.v4.widget.DrawerLayout>

